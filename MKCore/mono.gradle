plugins {
    id "mk-platform"
}


version = "${minecraft_version}-${mkcore_version}"
project.ext.mod_version = mkcore_version
project.ext.dependentProjects = [
        project(":TargetingAPI"),
        project(":MKWidgets")
]

apply from: '../common.gradle'
