plugins {
    id "com.google.cloud.artifactregistry.gradle-plugin" version "2.1.5"
    id "mk-platform"
}


version = "${minecraft_version}-${mkchat_version}"
project.ext.dependentProjects = [
        project(":MKWidgets"),
        project(":MKCore")
]

apply from: '../common.gradle'
