plugins {
    id "mk-platform"
}


version = "${minecraft_version}-${mkfaction_version}"
project.ext.mod_version = mkfaction_version
project.ext.dependentProjects = [
        project(":TargetingAPI"),
        project(":MKWidgets"),
        project(":MKCore")
]

apply from: '../common.gradle'

idea.module {
    excludeDirs << file("run")
}
