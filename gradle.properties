# Sets default memory used for gradle commands. Can be overridden by user or command line properties.
# This is required to provide enough memory for the Minecraft decompilation process.
org.gradle.jvmargs=-Xmx3G
org.gradle.daemon=false
org.gradle.parallel=true
org.gradle.caching=true
org.gradle.configuration-cache=true

java_version=21
minecraft_version=1.21.1
#TEMP for apple silicon fix
mixin_version=0.8.7

parchment_minecraft_version=1.21
parchment_mappings_version=2024.07.28
# Environment Properties
# You can find the latest versions here: https://projects.neoforged.net/neoforged/neoforge
# The Minecraft version range can use any release version of Minecraft as bounds.
# Snapshots, pre-releases, and release candidates are not guaranteed to sort properly
# as they do not follow standard versioning conventions.
minecraft_version_range=[1.21,1.21.1)
# The Neo version must agree with the Minecraft version to get a valid artifact
neo_version=21.0.167
# The Neo version range can use any version of Neo as bounds
neo_version_range=[21.0.0-beta,)
# The loader version range can only use the major version of FML as bounds
loader_version_range=[4,)

#mappings_channel=official
#mappings_version=1.19.2
#mappings_channel=parchment
#mappings_version=2022.12.18-1.19.3
# mappings_channel=snapshot
mappings_version=2024.07.28-1.21
mappings_channel=parchment
#mappings_version=1.19.4
mod_authors=MK Ultra Team

mod_description=Part of the MK Ultra Experience

mod_license=MIT

mod_vendor=chaosbuffalo

targeting_api_version=0.30

mk_widgets_version=1.19.1

mk_core_version=1.20

mk_chat_version=0.16

mk_faction_version=0.20

mk_weapons_version=0.15

mknpc_version=0.16

mkultra_version=1.1.0

